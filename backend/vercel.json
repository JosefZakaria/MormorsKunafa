{
  "version": 2,
  "framework": null,
  "installCommand": "cd .. && npm ci",
  "buildCommand": "npm run build && node -e \"const fs = require('fs'); fs.mkdirSync('public', { recursive: true }); fs.writeFileSync('public/index.html', 'OK');\"",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
