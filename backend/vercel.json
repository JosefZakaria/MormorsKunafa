{
  "version": 2,
  "framework": null,
  "installCommand": "cd .. && npm ci",
  "buildCommand": "npm run build && node -e \"require('fs').mkdirSync('public', { recursive: true })\"",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
