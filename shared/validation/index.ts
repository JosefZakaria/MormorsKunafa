/**
 * Shared Validation
 * 
 * This module contains validation logic that can be used by both
 * frontends and the backend to ensure consistent data validation.
 * 
 * IMPORTANT: Do not add any UI framework dependencies here.
 * Validation should be pure functions only.
 */

// Example validation helpers (to be implemented):

// export function isValidEmail(email: string): boolean {
//   const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
//   return emailRegex.test(email);
// }

// export function isValidPhoneNumber(phone: string): boolean {
//   // Norwegian phone number validation
//   const phoneRegex = /^(\+47)?[0-9]{8}$/;
//   return phoneRegex.test(phone.replace(/\s/g, ''));
// }

// export interface ValidationResult {
//   valid: boolean;
//   errors: string[];
// }

// export function validateOrder(order: unknown): ValidationResult {
//   const errors: string[] = [];
//   // Add validation logic here
//   return { valid: errors.length === 0, errors };
// }

