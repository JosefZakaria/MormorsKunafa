/**
 * Shared Types
 * 
 * This module contains TypeScript types and interfaces that are shared
 * across the web frontend, mobile frontend, and backend.
 * 
 * IMPORTANT: Do not add any UI-specific code here.
 * Types should be pure data structures only.
 */

// Example shared types (to be implemented based on your domain):

// export interface User {
//   id: string;
//   email: string;
//   name: string;
//   createdAt: string;
// }

// export interface Order {
//   id: string;
//   userId: string;
//   items: OrderItem[];
//   status: OrderStatus;
//   createdAt: string;
// }

// export type OrderStatus = 'pending' | 'confirmed' | 'preparing' | 'ready' | 'delivered';

